/**
 * App entry point — hands off to expo-router.
 *
 * LEARNING: expo-router uses a file-based routing system (like Next.js App Router).
 * All routes live in src/app/. This file is just the bridge between
 * React Native's native entry point and expo-router's navigation system.
 */
import "expo-router/entry";

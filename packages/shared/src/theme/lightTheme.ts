import { colors, spacing, radius, typography, rnShadows } from "../tokens";
import type { Theme } from "./types";

export const lightTheme: Theme = {
  colors,
  spacing,
  radius,
  typography,
  shadows: rnShadows,
};
